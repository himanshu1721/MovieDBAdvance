import { StyleSheet } from 'react-native';
import { scale } from '../../../themes';

const styles = StyleSheet.create({
  listFooterComponent: { width: scale(10) },
  itemSeparator: { width: scale(25) },
});

export default styles;
