import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  subContainer: {
    padding: 10,
    backgroundColor: '#000',
    alignItems: 'center',
  },
  footerStyles: {height: 100},
  container: {backgroundColor: '#000', flex: 1},
});

export default styles;
